<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Kuncen" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">#FF0000</item>
        <item name="android:navigationBarColor">#FF0000</item>
        <item name="android:statusBarColor">#FF0000</item>
        <item name="android:textColorHighlight">#FF0000</item>
        <item name="backgroundColor">@color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:fontFamily">@font/jetbrains_mono_semibold</item>


    </style>

    <style name="SplashScreenTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowBackground">@drawable/shield</item>
    </style>

    <style name="Theme.Kuncen" parent="Base.Theme.Kuncen" />
</resources>